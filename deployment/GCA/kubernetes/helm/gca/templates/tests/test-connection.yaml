apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gca.fullname" . }}-test-connection"
  labels:
    {{- include "gca.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gca.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
